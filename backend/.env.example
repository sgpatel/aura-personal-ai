# Database URL (adjust for your setup)
# Example for PostgreSQL:
#DATABASE_URL=postgresql://your_db_user:your_db_password@localhost:5432/aura_db
DATABASE_URL=postgresql://aura:aura@localhost:5432/auradb

# Secret key for JWT encoding (generate a strong random key)
# Example generation: python -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=your_strong_generated_secret_key_here

# Optional: Set log level (e.g., INFO, DEBUG)
LOG_LEVEL=INFO

# Optional: AI Service API Keys (if needed)
# OPENAI_API_KEY=sk-...